const getters = {
  menuCollapse: state => state.menu.menuCollapse,
  menuListState: state => state.menu.menuListState,
  currentSelectedIndex: state => state.menu.currentSelectedIndex
}

export default getters
